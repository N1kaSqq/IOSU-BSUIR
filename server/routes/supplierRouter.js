const { Router } = require('express');
const router = new Router();

/*  /api/suppliers/ */

router.post('/', );
router.get('/', );
router.put('/:id', );
router.delete('/:id', );

module.exports = router;