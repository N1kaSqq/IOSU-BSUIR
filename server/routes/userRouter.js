const { Router } = require('express');
const router = new Router();

/*  /api/users/ */

router.post('/register', );
router.post('/login', );
router.get('/isAuth', );
router.get('/', );
router.get('/:id', );
router.delete('/:id', );

module.exports = router;