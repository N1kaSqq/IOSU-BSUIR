const { Check } = require('../models/models');
const ApiError = require('../error/ApiError');

class CheckController {
    async create(req, res) {
        
    }

    async getAll(req, res) {
        
    }

    async getCheckById(req, res) {
        
    }
}

module.exports = new CheckController();